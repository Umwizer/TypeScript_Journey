let sales = 123_456_789; //in ts if you have large value of
// //number you can separate them using underscore to make them more readable.
// //because we assigned the value to number automatically Js make t number
let course = "TypeScript";
// let is_published = true;
// let level; //ts assumes this available can take any value;
// // level = 1;
// // level = "a"; // as see it taked different values
// //but as you know this will be against the use of including typescript
// function render(document: any) {
//   console.log(document);
// }

//typescript arrays
let numbers: number[] = [];
numbers.forEach((n) => n.toExponential);
